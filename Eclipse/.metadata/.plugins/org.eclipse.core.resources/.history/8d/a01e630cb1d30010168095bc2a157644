package edu.error;

import data.CodeSection;

public class LexerError extends error.IError {
    public LexerError(CodeSection section, String msg) {
    		super(section, msg);
    }
    public LexerError(int line, int offset, String msg) {
    		super(new CodeSection(line, offset, line, offset), msg);
    }
}
